name: Build Flutter APK

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout Code
        uses: actions/checkout@v4

      - name: ☕️ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🔧 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: 📦 Install Dependencies
        run: flutter pub get

      # 🎯 الخطوة الأهم: استخراج بصمة الـ SHA لربطها بـ Firebase
      - name: 🔑 Get SHA Fingerprints
        run: |
          cd android
          ./gradlew signingReport

      - name: 🏗️ Build Release APK
        # استخدمنا --debug هنا لأنك في مرحلة التيست وتستخدم توقيع الديباج في الـ gradle
        run: flutter build apk --debug

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: build/app/outputs/flutter-apk/app-debug.apk

